<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/darker_gray" /> <!-- color de fondo del círculo -->
    <corners android:radius="180dp" /> <!-- radio que define el círculo (ajustar al tamaño que desees) -->
</shape>
